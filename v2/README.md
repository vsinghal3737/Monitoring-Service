# Monitoring-Service v2

This folder is reserved for the new iteration of Monitoring-Service.

Current intent:

- keep `v1/` as the preserved reference implementation
- design `v2/` deliberately instead of porting legacy structure blindly
- add the new package layout, tooling, and tests here as the next tasks are defined
