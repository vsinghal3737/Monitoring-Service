# Monitoring-Service

Monitoring-Service is now organized as a two-track workspace:

- `v1/` keeps the original legacy implementation intact for reference and selective fixes.
- `v2/` is the clean area for the next iteration of the project.

## Repo Layout

```text
Monitoring-Service/
  v1/        # archived legacy implementation
    src/
    test/
  v2/        # new implementation area
```

## Notes

- The original project documentation was preserved under `v1/README.md`.
- New work should default to `v2/` unless a task explicitly targets `v1/`.
- Shared Claude/GPT handoffs for this repo belong in `E:\Study\Projects\handoff-notes`.
